export const metadata = {
  title: 'Blog | Game Artist Portfolio',
  description: 'Articles, tutorials, and case studies on casino slot game art.',
};

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
